  // const allModifiers = Object.entries(model).map(([, modifiers]) => modifiers);
  // const mergedModifiers: EmModifiers = merge({}, ...allModifiers);
  // const obj = { ...mergedModifiers } as const;

  //https://microsoft.github.io/TypeScript-New-Handbook/everything/#indexed-access-types
  // type RetFnProps = { [keyof obj]}
